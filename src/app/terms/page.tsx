import { PolicyStub } from "@/components/policy-stub";

export const metadata = { title: "Terms — LastMilePrep" };

export default function TermsPage() {
  return (
    <PolicyStub
      title="Terms & Conditions"
      blurb="Our full terms of service are being finalised ahead of launch. LastMilePrep provides exam practice and diagnostic tooling for educational use."
    />
  );
}
