import { PolicyStub } from "@/components/policy-stub";

export const metadata = { title: "Contact — LastMilePrep" };

export default function ContactUsPage() {
  return (
    <PolicyStub
      title="Contact us"
      blurb="A proper contact form is on the way. For now, reach out to the LastMilePrep team and we'll get back to you."
    />
  );
}
